# Generated lockfile, owned by pnpm.
pnpm-lock.yaml

# Scraped catalogue. Written one song per line by `pnpm fetch:songs` to keep
# refreshes reviewable; Prettier would spread every song over six lines.
data/songs.json

# Collected API evidence, written by `pnpm collect:evidence`.
data/pilot/artist-evidence.json
data/pilot/tail-evidence.json

# Offline match against the MusicBrainz canonical dump, written by `pnpm match:canonical`.
data/canonical-matches.json

# The machine-resolved layer, written by `pnpm build:resolved`.
data/resolved.json

# The review queue, written by `pnpm build:resolved`. Generated already-formatted; letting
# Prettier realign its tables would only make every regeneration a diff.
data/review.md
data/overrides-review.md
data/proposals-review.md
data/artist-slugs-review.md

# Artist lookups, written by `pnpm fetch:artists`.
data/artists.json

# Recording dates, written by `pnpm fetch:recordings`.
data/recordings.json
